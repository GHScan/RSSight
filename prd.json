{
  "project": "RSSight",
  "branchName": "feature/ai-rss-reader",
  "version": 1,
  "notes": "Windows + Cursor CLI (agent) + Ralph method (no upstream bash script). One iteration handles one story.",
  "userStories": [
    {
      "id": "S069",
      "priority": 34,
      "title": "Add date and color marker before read-later article titles",
      "description": "In the read-later article list, render a date label and color marker box to the left of each article title using the same logic as the Feed article list.",
      "acceptanceCriteria": [
        "Each read-later item shows date and color marker box before the title.",
        "Date rendering logic matches Feed article list behavior.",
        "Color marker logic matches Feed article list behavior.",
        "Visual ordering is date + color marker + title from left to right.",
        "Relevant frontend tests are updated or added, and pass."
      ],
      "passes": true
    }
  ]
}
